export interface Curso {
    titulo: string;
    progresso: number;
    conquistas: string[]; // Alterado para ser uma matriz de strings
    texto: string;
    cor: string;
    agrupamento: string;
}
